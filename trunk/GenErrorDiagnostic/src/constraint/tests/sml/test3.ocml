type 'a alist  = Nil 
              | Cons of 'a * 'a alist
                   
let x = (
  let rec map l f = (* should be map f l *)
    match l with  
      Nil -> Nil 
    | Cons (x,q) -> Cons (f x, map f q)    
    in map (fun x -> 1) Nil
  )
